# 站点阅读改造第六轮验收报告

日期：2026-09-08  
基线：HEAD `607e96f` 加当前未提交修改；site/ 共 23 个文件（+360 / -83），资源版本 `20260908-4`  
对照：[第五轮报告第 2 节及第 5 节](site-reading-redesign-review-2026-09-08-round-5.md)

**结论：通过，APPROVE。T2-c 和 P3 均已修复；第四轮 8 个定位查询、第五轮 §3.2 的 10 个样本以及 T1/T3/T4 回归抽查均通过。此次检查未发现需要继续返工的问题。**

本轮只新增验收报告并同步 index.md、log.md。保留全部未提交实现及历史报告，没有修改站点实现、知识正文、静态索引或复测记录，没有提交或部署。

## 1. 验收范围与结果

| 检查项 | file:// | 本地 HTTP | 结论 |
|---|---|---|---|
| `reward 信号`，375×667，首次点击正文 | 完整短语及上下文直接可见 | 同左 | 通过 |
| `reward 信号`，390×844，首次点击正文 | 完整短语及上下文直接可见 | 同左 | 通过 |
| T2-c 实际选择的 Range | U-OPSD 段的完整 `reward 信号` | 同左 | 通过；没有选择前段分散词 |
| 第四轮 8 个定位查询，390×844 | 8/8 | 8/8 | 通过 |
| 第五轮 §3.2 的 10 个样本，390×844 | 10/10 | 10/10 | 通过，含跨行内标签与非连续多词 |
| `token 汇聚` 的匹配等级 | 完整短语 | 完整短语 | 通过；不再选择前段窗口 |
| P3：VST 首页高亮 | 原文一致，无标签残片 | 同左 | 通过 |
| T1：来源状态往返 | 三组通过 | 三组通过 | 未见回归 |
| T3：回忆隐藏及退出恢复 | 三篇通过 | 三篇通过 | 未见回归 |
| T4：比较依据与焦点 | 两篇及跨页路径通过 | 同左 | 未见回归 |

浏览器：Chrome 152，独立浏览上下文，浅色，手机触控视口。入口：

```text
file:///Users/arno/Documents/50_knowledge_base/llm-wiki/site/index.html
http://127.0.0.1:4173/index.html
```

定位验收从首页实际渲染的结果链接进入，读入完成后直接测量目标正文的 DOM Range；测量前没有手动滚动正文。关键失败案例另通过搜索框输入、浏览器点击和首屏截图核对。随后以独立导航补充观察 `locateHitText()` 调用时的真实 Range，确认匹配等级和所属段落；该观察只包装测量 API 并原样返回其结果，没有修改正文或滚动逻辑，未用它代替首次点击验收。

## 2. T2-c 关闭依据

### 2.1 `reward 信号`：两尺寸、双通道通过

复验路径：打开首页 → 搜索 `reward 信号` → Open-MOPD 卡片 → 第二条 **“命中 全文 · 关联”** → 完整笔记 `#relations`。

首页片段对应 U-OPSD 关联段中的“但角色是 PPO 的 reward 信号（sg 停梯度、走 policy gradient + clip），不是直接蒸馏损失”。实际选择的 Range 文本为 **`reward 信号`**，所属 li 以 **U-OPSD** 开头，与片段一致；不是前一条 S²VOPD 的分散“信号”和“reward”。

| 视口 | file:// 命中文字 y | HTTP 命中文字 y | 初次点击结果 |
|---|---|---|---|
| 375×667 | 227.9～245.4px | 227.9～245.4px | 位于顶栏下，PPO reward 解释直接可读 |
| 390×844 | 669.5～687.0px | 669.5～687.0px | 完整短语与后续解释均在首屏 |

顶栏底部为 78px。375×667 的第五轮失败落点原为 y≈723～741px，本轮已移入可视区域。390×844 仍会保留“关联”标题附近的上下文，这是现有标题对齐规则的结果；补充观察已确认它选择的是 U-OPSD 的完整短语，不能仅凭滚动位置与上轮相同就推断仍选错。

### 2.2 `token 汇聚`：完整短语级命中

首页搜索 `token 汇聚` → GenLIP 的 **“全文 · 关键机制”**。

- 首页片段来自“模型发现‘只往少数枢纽 token 汇聚’就能预测”的段落。
- 两通道实际 Range 都是 **`token 汇聚`**，所属段为“生成式损失作用在逐 token 的 next word prediction 上……”。
- 命中文字位于 y≈101.9～119.4px；不再接受前面“token 上汇聚”的局部窗口。首页片段与正文选择一致。

### 2.3 代码与非连续查询复核

[wiki-slides.js](../../site/assets/wiki-slides.js) 第 1020～1051 行分为三个独立阶段：全部候选块的完整短语、全部候选块中跨度最小且不超过 600 的覆盖窗口、单词降级。完整短语按文档序返回；窗口仅在严格更小时替换 best，因此平局保留文档序。

首页 `matchSelection()` 同样优先完整短语，之后使用覆盖窗口；已复验的完整短语片段与落点一致。对没有连续短语的 `reward 新鲜度`，补充观察选中的 Range 在 U-OPSD 段内，从“新鲜度三层预算分配”覆盖到“本文 dense reward”，两词都在同一局部上下文内；没有降级成前段单独的 reward。`rollout 预算`、`token 记更多上下文` 也能直接读到包含全部查询词的上下文。

跨 strong/code/a 的原有 Range 映射、顶栏偏移和来源状态均保留。当前真实正文没有 em 标签实例，本轮不宣称覆盖了真实 em 样本。

## 3. 定位回归明细

以下均从首页命中链接进入，390×844，file:// 与 HTTP 结果一致。y 为命中文字范围，取整数便于复查；非连续多词按同段内的实际词位置记录。

### 3.1 第四轮 8 个查询

| 查询 | 页面／章节 | 命中文字 y | 结果 |
|---|---|---|---|
| `Dataset-S2` | GenLIP / mechanism | 102～120 | 通过 |
| `59.01` | U-OPSD / evidence | 102～120 | 通过 |
| `取最后 LN 层输出` | GenLIP / mechanism | 102～147 | 通过 |
| `不需要另一次 prefill 就能清掉的那部分陈旧` | Open-MOPD / mechanism | 102～146 | 通过 |
| `PPO 每次内更新本来就要` | Open-MOPD / mechanism | 102～119 | 通过 |
| `比如同时往 200 和 800 两个位置探头` | LocateAnything / pitfalls | 388～405 | 通过 |
| `关键在梯度路由方式` | GenLIP / mechanism | 102～119 | 通过 |
| `token 自由双向交互` | GenLIP / mechanism | 102～119 | 通过 |

### 3.2 第五轮 10 个新样本

| 查询 | 页面／章节 | 命中文字 y 或匹配方式 | 结果 |
|---|---|---|---|
| `涂黑成 [MASK]` | LocateAnything / intuition | 102～147，跨 code | 通过 |
| `都是 </Silence>` | VideoChat3 / mechanism | 502～546，跨 code | 通过 |
| `和 U-OPSD 的分工` | S²VOPD / problem | 534～552，跨链接两侧 | 通过 |
| `系统 VideoCrop 执行裁剪` | Video-o3 / mechanism | 380～399，跨 code 两侧 | 通过 |
| `到第 K 段用户提问 q` | VST / mechanism | 435～452，行内 q 投影为 code | 通过 |
| `token 汇聚` | GenLIP / mechanism | 102～119，完整短语级命中 | 通过 |
| `reward 信号` | Open-MOPD / relations | 670～687，完整短语；较矮视口另见第 2 节 | 通过 |
| `rollout 预算` | U-OPSD / evidence | 两词同在 383～400，原文为非连续词 | 通过 |
| `token 记更多上下文` | VST / restatement | 两词同在 604～622，原文为“token数记更多上下文” | 通过 |
| `reward 新鲜度` | Open-MOPD / relations | U-OPSD 段的新鲜度 481～498、reward 643～660 | 通过 |

`涂黑成 [MASK]` 的真实首页链接指向 `#intuition`，表中按实际入口记录；没有强行导航到其他章节。三个非连续多词查询没有被误记为完整短语匹配。

## 4. P3 关闭依据

路径：两通道首页搜索 **`到第 K 段用户提问 q`**，查看 VST 的 **“全文 · 关键机制”** 片段。

本轮可见文字正确保留：

```text
到第 K 段用户提问 $q$，直接拿「全部笔记 + 当前画面」生成答案，不再做 post-query 长推理。
```

从浏览器取出片段可见文本，去掉“命中”标题和两端截断省略号后，与 [VST Markdown](../../wiki/papers/2026-vst.md) 折叠空白后的文本比较：**两通道均为真源中的完整连续子串**。不仅检查了查询短语，也检查了整段截取内容。

DOM 子元素仅有正常的 STRONG 和 MARK，高亮仍正常；没有 `k>`、`<mar<mark>` 或其他标签残片。`$q$` 是源文已有的行内数学写法，不是损坏的 HTML。[highlight()](../../site/assets/wiki-slides.js) 第 44～79 行先在原文收集、合并区间，再转义生成 HTML，不再对生成后的标签做 token 替换。P3 关闭。

## 5. T1、T3、T4 回归抽查

**T1：通过。** 两通道各检查三组 `q / topic / tag / review`：

- `stop-gradient / distillation / self-distillation / due` → U-OPSD。
- `token 自由双向交互 / visual-encoders / generative-pretraining / due` → GenLIP。
- `y+ / distillation / self-distillation / due` → U-OPSD。

从首页文章链接进入完整笔记或速览，再走完整笔记 → 速览 → 完整笔记 → 知识库。file:// 使用完整笔记顶部速览出口，HTTP 使用页脚出口；返回完整笔记使用手机“完整笔记”入口。每一步四字段均保持，返回后输入框正确，原结果恢复为 U-OPSD、GenLIP、U-OPSD + S²VOPD。

为让 due 筛选有结果，在起点及返回首页后的校验刷新中使用上海 2026-10-07 的浏览器 Date；返回点击本身先检查 URL 字段，随后在相同日期下比较结果。没有改系统日期、索引或复测记录。

**T3：通过抽查。** 两通道各检查 GenLIP、U-OPSD、S²VOPD 的 `recall=1`。初始 mobile-cover-takeaway、问题、答案均不可见；显示问题后分别有 2/4/3 题，答案仍隐藏；打开第一题仅一份答案可见；退出后核心提示恢复，回忆状态清除。可见性使用 `checkVisibility()` 核对。

**T4：通过抽查。** 两通道在 U-OPSD 与 S²VOPD 比较弹窗内，用 Enter 分别激活同页 `#qa-y-plus`、`#qa-divergence`。弹窗关闭、答案展开，焦点落在目标 `.qa-toggle`，目标顶部 y=102px。Esc 关闭弹窗后焦点回比较按钮。S²VOPD → U-OPSD 的跨页问答依据及 U-OPSD → S²VOPD 完整笔记 `#open` 也可读，目标顶部均为 102px，没有弹窗遮挡。

## 6. 工程检查与验收边界

- wiki-slides.js、wiki-index.js、build-wiki-index.mjs 的 `node --check` 通过；`git diff --check` 通过。
- 21 个 HTML 的 53 处共享资源引用全部为 `20260908-4`。
- wiki-index.js、九篇 Markdown、review.md 相对 HEAD 均未修改。索引 SHA-256 为 `55ed67ec755ad7f80ffef390b9cddf477ddf67eac3c820676df134c74498c125`。
- site/ diff 在本轮验收前后 SHA-256 均为 `1fd1f74dc6e6e2a53b9b42e0e3c95b3beed68f2fb9a3dd4c5b5f92965f80f0bd`，未触碰用户实现。
- 收尾抽查页面及其保留的最近三次导航控制台无 error/warn；未将此表述为全部历史导航的日志审计。
- 本轮按第五轮收尾标准执行，没有重跑第三轮全库索引覆盖和生成器幂等，也没有把 T1/T3/T4 抽查扩大表述为全量验证。未验证 Safari、真实 iOS 或线上部署。
- 4173 起初无监听，本轮临时启动仅绑定 127.0.0.1 的静态服务；完成后关闭该服务和本轮测试页。

**本轮列出的收尾条件全部满足，T2-c 与 P3 关闭，没有新增返工项。此结论针对上述未提交工作区版本。**
